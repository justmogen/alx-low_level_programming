#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/**
 * Desc: Header file that defines macro ABS(x)
 * that finds absolute value of a number x
 *
 */
#define ABS(x) ((x) < 0 ? (-x) : (x))


#endif
